import java.util.Arrays;import java.util.Scanner; public class Main {     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();         while (t-- > 0) {            int n = scanner.nextInt();            int[] a = new int[n];            for (int i = 0; i < n; i++) {                a[i] = scanner.nextInt();            }             int[] b = a.clone();            Arrays.sort(b);             boolean alreadySorted = true;            for (int i = 0; i < n; i++) {                if (a[i] != b[i]) {                    alreadySorted = false;                    break;                }            }             if (alreadySorted) {                System.out.println(-1);                continue;            }             int ans = Integer.MAX_VALUE;            int max_value = b[0];            int min_value = b[n - 1];            for (int i = 0; i < n; i++) {                if (a[i] != b[i]) {                    int p = Math.max(Math.abs(a[i] - max_value), Math.abs(a[i] - min_value));                    ans = Math.min(ans, p);                }            }             System.out.println(ans);        }    }}