import java.util.Scanner; public class Main {     public static void reverse(int[] arr, int l, int r) {        while (l < r) {            int temp = arr[l];            arr[l] = arr[r];            arr[r] = temp;            l++;            r--;        }    }     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();        while (t-- > 0) {            int n = scanner.nextInt();            int[] arr = new int[n];            for (int i = 0; i < n; i++) {                arr[i] = scanner.nextInt();            }             int[] maxArr = new int[n];            int[] maxIdx = new int[n];             maxArr[n - 1] = arr[n - 1];            maxIdx[n - 1] = n - 1;             for (int i = n - 2; i >= 0; i--) {                if (arr[i] >= maxArr[i + 1]) {                    maxArr[i] = arr[i];                    maxIdx[i] = i;                } else {                    maxArr[i] = maxArr[i + 1];                    maxIdx[i] = maxIdx[i + 1];                }            }             for (int i = 0; i < n; i++) {                if (maxArr[i] > arr[i]) {                    reverse(arr, i, maxIdx[i]);                    break;                }            }             for (int i = 0; i < n; i++) {                System.out.print(arr[i] + " ");            }            System.out.println();        }    }}