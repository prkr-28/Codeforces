import java.util.Scanner; public class Mian {     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();        while (t-- > 0) {            int n = scanner.nextInt();            int[] arr = new int[n];            for (int i = 0; i < n; i++) {                arr[i] = scanner.nextInt();            }             int cnt = 0;            int negone = 0;            int posone = 0;            for (int i = 0; i < n; i++) {                if (arr[i] == -1) {                    negone++;                } else if (arr[i] == 1) {                    posone++;                }            }            while (negone > posone) {                negone--;                posone++;                cnt++;            }            if (negone % 2 == 1) {                cnt++;                negone--;                posone++;            }            System.out.println(cnt);        }    }}