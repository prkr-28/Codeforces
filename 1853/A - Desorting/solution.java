import java.util.Scanner; public class Main {     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();        while (t-- > 0) {            int n = scanner.nextInt();            int[] arr = new int[n];            for (int i = 0; i < n; i++) {                arr[i] = scanner.nextInt();            }             int min = Integer.MAX_VALUE;             boolean alreadyUnSorted = false;            for (int i = 0; i < n - 1; i++) {                int diff = arr[i + 1] - arr[i];                if (diff < 0) {                    alreadyUnSorted = true;                    break;                }                int ops = diff / 2 + 1;                min = Math.min(min, ops);            }             if (alreadyUnSorted) {                System.out.println(0);            } else {                System.out.println(min);            }        }    }}