import java.util.HashSet;import java.util.Scanner;import java.util.Set; public class Main {     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();         while (t-- > 0) {            int a = scanner.nextInt();            int b = scanner.nextInt();            int xk = scanner.nextInt();            int yk = scanner.nextInt();            int xq = scanner.nextInt();            int yq = scanner.nextInt();             int[][] moves = {                {a, b}, {a, -b}, {-a, b}, {-a, -b},                {b, a}, {b, -a}, {-b, a}, {-b, -a}            };             Set<String> kingReach = new HashSet<>();            Set<String> queenReach = new HashSet<>();             for (int[] m : moves) {                int nx = xk + m[0];                int ny = yk + m[1];                kingReach.add(nx + "," + ny);             }             for (int[] m : moves) {                int nx = xq + m[0];                int ny = yq + m[1];                queenReach.add(nx + "," + ny);            }             // Count intersection            int count = 0;            for (String pos : kingReach) {                if (queenReach.contains(pos)) {                    count++;                }            }             System.out.println(count);        }    }}