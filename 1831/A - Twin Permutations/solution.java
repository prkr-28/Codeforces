import java.util.Scanner; public class Main {     public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int t = scanner.nextInt();         while (t-- > 0) {            int n = scanner.nextInt();            int[] arr = new int[n];            int maxVal = Integer.MIN_VALUE;            for (int i = 0; i < n; i++) {                arr[i] = scanner.nextInt();                if (arr[i] > maxVal) {                    maxVal = arr[i];                }            }            maxVal++;            for (int i = 0; i < n; i++) {                System.out.print(maxVal - arr[i] + " ");            }            System.out.println();        }    }}